import { Garage } from "@/components/garage";

export const metadata = {
  title: "The Garage — The Pit Wall",
  description: "Explode the kit, then drive — a lite homage to the Formula lab.",
};

export default function GaragePage() {
  return <Garage />;
}
